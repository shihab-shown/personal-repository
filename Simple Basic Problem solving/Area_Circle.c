#include <stdio.h>

    int main()
{

        float pi= 3.1416;
        float radius;
        printf("Input the value of Radius\n");
        scanf("%f", &radius);
        printf("The Area of the Circle is %f", pi*radius*radius);

    return 0;

}